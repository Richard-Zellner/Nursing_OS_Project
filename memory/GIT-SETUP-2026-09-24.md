# GitHub setup — 2026-09-24

Owner-authorized destination: https://github.com/Richard-Zellner/Nursing_OS_Project

Visibility: **public**. Branch: `main`.

The shared controller now supports committing and pushing each accepted
work unit after independent verification. Windows Git Credential Manager
holds the credential; workers do not receive it. Commit identity is local
to this repository: Richard Zellner,
`194161347+Richard-Zellner@users.noreply.github.com`.

The initial source baseline includes existing owner work and is separate
from future `loop: <work-id>` commits. Review, commit and push owner edits
between runs. A dirty or divergent baseline waits for reconciliation.
A failed push retains accepted work; retry with
`../loops/controller.ps1 git-sync --project Nursing_OS_Project`.

Project enablement, milestones, release gates and creative review
checkpoints retain their existing meaning. Repository creation is not
a release or approval of draft creative material.
